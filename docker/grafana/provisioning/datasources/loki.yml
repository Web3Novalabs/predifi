apiVersion: 1

datasources:
  - name: Loki
    uid: predifi-loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
    jsonData:
      maxLines: 1000
      # Surfaces the Loki ruler alert rules in Grafana's alerting UI.
      manageAlerts: true
