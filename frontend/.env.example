# Base URL of the predifi backend API (local backend runs on port 8080)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Stellar network to connect to (testnet | mainnet)
NEXT_PUBLIC_STELLAR_NETWORK=testnet
